"""
ecgclean.active_learning — Active learning loop for ECG artifact annotation.

Submodules:
    sampler           — Candidate selection strategies (margin, committee, priority, combined)
    annotation_queue  — Export/import annotation queue for the annotation UI
"""
